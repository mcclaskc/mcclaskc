<h1>Listing tournynames</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @tournynames.each do |tournyname| %>
  <tr>
    <td><%= link_to 'Show', tournyname %></td>
    <td><%= link_to 'Edit', edit_tournyname_path(tournyname) %></td>
    <td><%= link_to 'Destroy', tournyname, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Tournyname', new_tournyname_path %>
